# Reporting layer

Reserved for deterministic quarterly aggregation and template-driven report generation after extraction accuracy is established. Reporting code will consume approved `ServiceEvent` records; it will not parse source PDFs directly.

